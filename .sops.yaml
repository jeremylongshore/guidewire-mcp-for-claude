# SOPS encryption rules for Guidewire MCP for Claude.
#
# Per Intent Solutions standard (SOPS + age, never Vaultwarden / GCP
# Secret Manager). Decrypt creds in-process via the sops-env wrapper
# script (lands in scripts/sops-env when sandbox creds materialize).
#
# Recipients: every engineer with write access to this repo's
# secrets has their age public key listed below. To rotate or add a
# recipient, edit this file + re-encrypt all *.sops.* files +
# commit.

creation_rules:
  # Per-environment secrets (committed encrypted)
  - path_regex: \.env\.sops$
    age: >-
      age1me3vkelljqe2u4zcagja9ru5fdpfpw72xmch39fwle2cr0yfr4cs8vr5d8

  - path_regex: secrets\.[a-z]+\.sops\.yaml$
    age: >-
      age1me3vkelljqe2u4zcagja9ru5fdpfpw72xmch39fwle2cr0yfr4cs8vr5d8

# To verify a recipient's public key fingerprint:
#   age-keygen -y < ~/.config/sops/age/keys.txt
# Recipient added 2026-05-04: Jeremy Longshore
#   age1me3vkelljqe2u4zcagja9ru5fdpfpw72xmch39fwle2cr0yfr4cs8vr5d8
