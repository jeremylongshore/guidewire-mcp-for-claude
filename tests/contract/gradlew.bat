@rem ============================================================================
@rem STUB — regenerate with: gradle wrapper --gradle-version 8.7
@rem ============================================================================
@rem This file is a placeholder. See gradlew (the Unix version) for the
@rem regeneration instructions. Run `gradle wrapper --gradle-version 8.7` from
@rem inside tests/contract/ to produce the real batch script.
@rem ============================================================================
@echo ERROR: gradlew.bat is a stub. Run "gradle wrapper --gradle-version 8.7" inside tests\contract\ to generate the real wrapper. 1>&2
@exit /b 1
